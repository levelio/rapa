{
  "name": "@rapa/ppu-monorepo",
  "private": true,
  "version": "0.0.1",
  "author": "chord",
  "keywords": [
    "scaffolding"
  ],
  "workspaces": [
    "packages/*"
  ],
  "description": "frontend project generate tool",
  "main": "index.js",
  "scripts": {
    "test": "lerna run test --stream",
    "build": "lerna run build --stream",
    "lint": "web-scripts lint --ignore-path .gitignore",
    "commit": "web-scripts commit",
    "bootstrap": "lerna bootstrap --use-workspaces"
  },
  "devDependencies": {
    "lerna": "^3.16.4"
  }
}
